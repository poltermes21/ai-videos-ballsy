node_modules

# Remotion bundle output and final render output
build
dist
out

# Logs
logs
*.log
npm-debug.log*

# Env
.env
.env.*
!.env.example

# TypeScript / ESLint caches
*.tsbuildinfo
.eslintcache

# OS
.DS_Store

# Local Claude Code config and private project notes — not for the public repo.
.claude
.agents
skills-lock.json

# Test TTS audio + derived lip-sync data — regenerable, and ElevenLabs free-tier
# audio has no commercial rights, so it shouldn't be committed.
public/audio/

# Generated match scripts — regenerable test/review output, not source.
scripts/output/

# Python sidecar (SofaScore) — local venv + bytecode caches
scripts/sofascore/.venv/
__pycache__/
*.pyc

# Match selector frontend — compiled from scripts/server/src/*.ts (every
# entry point: app.ts, youtube-publish-ui.ts, tiktok-publish-ui.ts, ...).
scripts/server/public/*.js
scripts/server/public/*.js.map

# OAuth refresh tokens for YouTube/TikTok publishing — real credentials.
scripts/server/.credentials/
